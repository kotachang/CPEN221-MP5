package ca.ece.ubc.cpen221.mp5;

import java.util.ArrayList;
import java.util.List;
import java.util.Set;

public class kMeansClusters {
	/*
	 * List<Cluster> sublist; List<Business> businesses;
	 * 
	 * public kMeansClusters(int n, List<Business> businesses) {
	 * 
	 * sublist = new ArrayList<Cluster>();
	 * 
	 * int k = 0; double distance = 0; int closest = 0;
	 * 
	 * for (int i = 0; i < n - 1; i++) { sublist.add(new
	 * Cluster(businesses.subList(k, n + k))); k += n; }
	 * 
	 * sublist.add(new Cluster(businesses.subList(k, businesses.size() - 1)));
	 * 
	 * for (int a = 0; a < businesses.size(); a++) { for (int b = 0; b <
	 * sublist.size(); b++) { if (distance <
	 * distance(businesses.get(a).getCoordinates(), sublist.get(b).findCenter())) {
	 * distance = distance(businesses.get(a).getCoordinates(),
	 * sublist.get(b).findCenter()); closest = b; } }
	 * sublist.get(closest).add(businesses.get(a)); } }
	 */
}
